# Docs

## How to load the docs?

1. Make sure NodeJS (Latest LTS) is installed
2. Run `./serve.sh`
3. Accept installing `serve` if prompted
4. Navigate to `localhost:3000` in your browser and select a site to preview


## Public Downloads

You can still download the original Debian 5.16.17 build from the Internet Archive. I have not tested this build, it's just something that I've stumbled across

https://archive.org/details/debian_bullseye_minimal_linux5.16.17_root_orangepi.img